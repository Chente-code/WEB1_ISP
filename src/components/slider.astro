---

---

<div
  class="slider-container relative overflow-hidden max-w-screen-xl mx-auto py-12 px-6 lg:px-8"
>
  <div class="slider flex transition-transform duration-500 ease-in-out">
    <div class="slide flex-shrink-0 w-full">
      <div class="bg-white p-6 rounded-lg shadow-lg text-center">
        <img
          class="w-24 h-24 object-cover rounded-full mx-auto mb-4"
          src="path_to_image/image1.jpg"
          alt="Testimonio 1"
        />
        <h2 class="text-2xl font-semibold text-gray-900 mb-2">Título 1</h2>
        <p class="text-gray-600">
          Este es un párrafo de ejemplo para el testimonio 1.
        </p>
      </div>
    </div>
    <div class="slide flex-shrink-0 w-full">
      <div class="bg-white p-6 rounded-lg shadow-lg text-center">
        <img
          class="w-24 h-24 object-cover rounded-full mx-auto mb-4"
          src="path_to_image/image2.jpg"
          alt="Testimonio 2"
        />
        <h2 class="text-2xl font-semibold text-gray-900 mb-2">Título 2</h2>
        <p class="text-gray-600">
          Este es un párrafo de ejemplo para el testimonio 2.
        </p>
      </div>
    </div>
    <div class="slide flex-shrink-0 w-full">
      <div class="bg-white p-6 rounded-lg shadow-lg text-center">
        <img
          class="w-24 h-24 object-cover rounded-full mx-auto mb-4"
          src="path_to_image/image2.jpg"
          alt="Testimonio 2"
        />
        <h2 class="text-2xl font-semibold text-gray-900 mb-2">Título 2</h2>
        <p class="text-gray-600">
          Este es un párrafo de ejemplo para el testimonio 2.
        </p>
      </div>
    </div>
    <div class="slide flex-shrink-0 w-full">
      <div class="bg-white p-6 rounded-lg shadow-lg text-center">
        <img
          class="w-24 h-24 object-cover rounded-full mx-auto mb-4"
          src="path_to_image/image2.jpg"
          alt="Testimonio 2"
        />
        <h2 class="text-2xl font-semibold text-gray-900 mb-2">Título 2</h2>
        <p class="text-gray-600">
          Este es un párrafo de ejemplo para el testimonio 2.
        </p>
      </div>
    </div>
    <!-- Añadir más slides según sea necesario -->
  </div>
  <button
    class="prev absolute top-1/2 transform -translate-y-1/2 left-0 bg-gray-300 hover:bg-gray-400 text-gray-700 rounded-full p-2"
  >
    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
      <path
        fill-rule="evenodd"
        d="M12.293 4.293a1 1 0 010 1.414L8.414 10l3.879 3.879a1 1 0 11-1.414 1.414l-4.586-4.586a1 1 0 010-1.414l4.586-4.586a1 1 0 011.414 0z"
        clip-rule="evenodd"></path>
    </svg>
  </button>
  <button
    class="next absolute top-1/2 transform -translate-y-1/2 right-0 bg-gray-300 hover:bg-gray-400 text-gray-700 rounded-full p-2"
  >
    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
      <path
        fill-rule="evenodd"
        d="M7.707 4.293a1 1 0 010 1.414L11.586 10l-3.879 3.879a1 1 0 001.414 1.414l4.586-4.586a1 1 0 000-1.414l-4.586-4.586a1 1 0 00-1.414 0z"
        clip-rule="evenodd"></path>
    </svg>
  </button>
</div>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const slider = document.querySelector(".slider");
    const slides = document.querySelectorAll(".slide");
    const nextButton = document.querySelector(".next");
    const prevButton = document.querySelector(".prev");
    let currentIndex = 0;

    // Verificar si nextButton y prevButton existen
    if (nextButton && prevButton) {
      nextButton.addEventListener("click", () => {
        currentIndex = (currentIndex + 1) % slides.length;
        showSlide(currentIndex);
      });

      prevButton.addEventListener("click", () => {
        currentIndex = (currentIndex - 1 + slides.length) % slides.length;
        showSlide(currentIndex);
      });
    }

    function showSlide(index) {
      const offset = -index * 100;
      slider.style.transform = `translateX(${offset}%)`;
    }

    showSlide(currentIndex);
  });
</script>
